# Dockerfile (open-webui/)
FROM ghcr.io/open-webui/open-webui:main

# Persistência: a imagem upstream escreve em /app/backend/data (montado no compose)
# Variáveis de ambiente podem ser declaradas aqui ou no compose
# ENV OLLAMA_BASE_URL=http://ollama:11434

EXPOSE 8081

# ENTRYPOINT/CMD herdados do upstream
