# Dockerfile (ollama/)
FROM ollama/ollama:latest

# Se precisar copiar modelos, configs ou scripts de entrada, descomente:
# COPY models/ /root/.ollama/models/
# COPY entrypoint.sh /usr/local/bin/entrypoint.sh
# RUN chmod +x /usr/local/bin/entrypoint.sh

# Porta padrão do servidor Ollama
EXPOSE 11434

# Comando padrão do upstream
# ENTRYPOINT e CMD já vêm definidos na imagem base
